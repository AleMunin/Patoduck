# RULES

- Always start with "print_"
- It has to be a snippet that only shows information, and is not called elsewhere

Example:

htmx/single_speech.html is purely informational. It however is always called by htmx/single_speech.html
It is then easier to find on htmx.