def conv_validation():
    ...
    # if a quest is selected, mark as is_quest
    # if not, mark as is_quest = False
    # if name = "to_be_named", it will just pick characters involved
    # or a %character% tag
    
    
    # import code from tree_of_speeches